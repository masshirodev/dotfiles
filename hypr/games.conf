# Hyprland Window Rules Reference
# ================================
#
# Basic syntax: windowrule = [RULE], [WINDOW]
# Advanced syntax: windowrulev2 = [RULE], [WINDOW]
#
# Window States:
# - tile          - tiled window
# - float         - floating window
# - fullscreen    - fullscreen window
# - maximize      - maximized window
# - minimize      - minimized window
#
# Window Properties:
# - opacity [value]           - set opacity (0.0-1.0)
# - size [width] [height]     - set window size
# - move [x] [y]              - set window position
# - center                    - center the window
# - pin                       - pin window (always on top)
# - workspace [number/name]   - move to specific workspace
# - monitor [number]          - force to specific monitor (0, 1, 2, etc.)
# - nofocus                   - don't focus when opened
# - noinitialfocus            - don't focus on first map
# - noblur                    - disable blur
# - noshadow                  - disable shadow
# - noborder                  - remove border
# - nodim                     - don't dim when unfocused
#
# Window Matching:
# - title:^(exact title)$     - match exact window title
# - class:^(exact class)$     - match exact window class
# - title:.*partial.*         - match partial title
#
# Examples:
# windowrule = tile, title:^(S.T.A.L.K.E.R.: Anomaly)$
# windowrule = monitor 1, title:^(S.T.A.L.K.E.R.: Anomaly)$
# windowrule = float, class:^(pavucontrol)$
# windowrule = center, class:^(pavucontrol)$
# windowrule = opacity 1.0, title:^(S.T.A.L.K.E.R.: Anomaly)$
#
# Multiple rules for same window:
# windowrule = tile, title:^(S.T.A.L.K.E.R.: Anomaly)$
# windowrule = monitor 1, title:^(S.T.A.L.K.E.R.: Anomaly)$
# windowrule = workspace 2, title:^(S.T.A.L.K.E.R.: Anomaly)$

# Open as
windowrule = tile on, match:title ^(Balatro)$
windowrule = tile on, match:title ^(Street Fighter 6)$
windowrule = fullscreen on, opacity 1.0, monitor 1, match:title ^(S.T.A.L.K.E.R.: Anomaly)$
windowrule = tile on, match:title ^(Bottles)$
windowrule = tile on, match:title ^(Mod Organizer)$
windowrule = tile on, match:title ^(Mod Organizer 2)$
windowrule = tile on, match:title ^(Retro Gadgets)$
windowrule = tile on, match:title ^(Quickie)$
windowrule = tile on, match:title ^(Amatsutsumi)$
windowrule = tile on, match:title ^(Steam)$
# windowrule = tile, title:^(Tabletop Simulator)$
windowrule = tile on, match:title ^(TheFarmerWasReplaced)$
windowrule = opacity 1.0, match:title (?i)(counter.?strike) # case insensitive match for "counter?strike"
windowrule = opacity 1.0, match:title ^(ARC Raiders)$

# Open in

# Steam hopefully opens my apps now
decoration:blur:enabled = false
animations:enabled = false
misc:vfr = true

# Fear & Hunger
# windowrule = tile on, match:title ^(Fear & Hunger)$

# Game files
source = ~/.config/myconf/hypr/games/rusty.conf
source = ~/.config/myconf/hypr/games/minicozyroom.conf
